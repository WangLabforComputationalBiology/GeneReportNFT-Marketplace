# GeneReportNFT-Marketplace
A decentralized NFT marketplace for trading genetic test reports

本地测试 => config.local.yaml
* `APP_ENV=local go run main.go`

线上环境(内网穿透) => config.tunnel.yaml
* `APP_ENV=tunnel go run main.go`